import { Starships } from "../../data/types";

interface StarShipProps {
  starships: Starships;
}

const Starship = ({ starships }: StarShipProps): JSX.Element => {
  return <div></div>;
};

export default Starship;
